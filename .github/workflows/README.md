# GitHub Actions Workflows

This directory contains CI/CD workflows for the TipStream SDK.

## Workflows

- `test.yml` - Run tests on push/PR
- `publish.yml` - Publish packages on release
- `metrics.yml` - Daily metrics collection
